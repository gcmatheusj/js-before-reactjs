/**
 * Funções no JavaScript são blocos de código que agrupa instruções para resolver 
 * uma determinada tarefa específica.
 */

function soma (x, y) {
  return x + y
}

console.log(soma(10, 20))

const subtracao = function (x, y) {
  return x - y
}

console.log(subtracao(20, 10))

